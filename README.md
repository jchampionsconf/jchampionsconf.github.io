# Website for the JChampions conference

## Development

Easiest way to see the website locally is to launch NGINX with docker to server this directory.

`docker run --rm -ti -p 8080:80 -v .:/usr/share/nginx/html:ro nginx`

